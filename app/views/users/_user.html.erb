
<div style="margin: 0 0 10px 0">
  <%= link_to profile_photo(id: user.id, size: :small,
                            styling: 'img-thumbnail custom-friend-pic'),
                            user_timeline_path(user) %>
                          <%= link_to user.name, user_timeline_path(user) %>
</div>
